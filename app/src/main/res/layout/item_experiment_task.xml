<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/task_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:textSize="14sp"
        android:textAlignment="textStart"
        android:gravity="start|center_vertical"
        app:cornerRadius="8dp"
        app:icon="@drawable/ic_task"
        app:iconGravity="start"
        app:iconPadding="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"/>

    <TextView
        android:id="@+id/task_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorPrimary"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginStart="56dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="@id/task_button"
        app:layout_constraintStart_toStartOf="@id/task_button"
        app:layout_constraintEnd_toEndOf="@id/task_button"
        android:layout_marginTop="12dp"/>

    <TextView
        android:id="@+id/task_deadline"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="?android:attr/textColorSecondary"
        android:layout_marginTop="4dp"
        android:layout_marginStart="56dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="12dp"
        app:layout_constraintTop_toBottomOf="@id/task_name"
        app:layout_constraintStart_toStartOf="@id/task_button"
        app:layout_constraintEnd_toEndOf="@id/task_button"
        app:layout_constraintBottom_toBottomOf="@id/task_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>
